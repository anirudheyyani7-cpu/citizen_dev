# Copy this file to .env and fill in your Anthropic API key.
# IMPORTANT: Never commit .env to version control.

# Used by server.js (relay) — keeps the key server-side only
ANTHROPIC_API_KEY=sk-ant-...
